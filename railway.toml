[build]
builder = "NIXPACKS"
buildCommand = "python3.11 -m pip install -r requirements.txt && npm install pptxgenjs docx"

[deploy]
startCommand = "python3.11 bot.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
